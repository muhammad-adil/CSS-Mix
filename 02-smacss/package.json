{
  "name": "sass_with_smacss_and_bem",
  "version": "0.0.1",
  "description": "- Install all the packages `npm install` - To compile the CSS and watch for the change, run `npm run watch` - To compile the CSS files and folder, run `npm run build`.",
  "main": "index.js",
  "scripts": {
    "watch": "node-sass --watch assets/sass -o assets/css",
    "build": "node-sass --output-style compressed assets/sass -o assets/css",
    "serve": "parcel index.html"
  },
  "keywords": [
    "Sass",
    "with",
    "Smacss",
    "&",
    "bem"
  ],
  "author": "Adil",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "node-sass": "^4.11.0",
    "parcel": "^1.9.4",
    "popper": "^1.0.1"
  }
}
